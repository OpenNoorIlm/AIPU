adapter driver remote_bitbang
remote_bitbang host localhost
remote_bitbang port 3335

adapter speed 1000

transport select jtag
jtag newtap gowin tap -irlen 8 -expected-id 0x0000081b

pld create gowin.pld gowin -tap gowin.tap

init
